name: animation_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  fluro: "^1.3.1" #loading_button

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.2"
  font_awesome_flutter: ^8.5.0


flutter:

  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "icon/icon.png"